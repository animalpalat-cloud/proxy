/**
 * Legacy API helpers removed — Ultraviolet uses same-origin /bare/ via Next rewrite.
 * URL encoding lives in lib/uvCodec.ts.
 */

export { buildProxiedPath, encodeUvUrl, normalizeTargetUrl } from "./uvCodec";
